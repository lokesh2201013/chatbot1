document.getElementById("loginForm").addEventListener("submit", function (e) {
    e.preventDefault();

    // Here you can perform Aadhar authentication and redirect to the main website.
    // For this example, we'll simply redirect to a placeholder URL.
    window.location.href = "INDEX.html";
});
